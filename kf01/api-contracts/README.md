# API 契约目录

本目录保存《往日重现》2.0 的跨端 API 契约。当前 `openapi.yaml` 是先行草案，用于统一 NestJS 过渡实现、未来 FastAPI 实现、Vben Admin 和 Android TV 的字段边界。

## 更新规则

- 新增接口先改契约，再写后端测试和实现。
- 跨端字段只增不删；必须删除时先做兼容期。
- TV 端接口要明确鉴权方式、缓存策略和媒体 URL 类型。
- Admin 端接口要明确缩略图字段，避免前端直接加载原图。
